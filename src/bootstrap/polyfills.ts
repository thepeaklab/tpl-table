import 'core-js/es6';

import 'reflect-metadata';

require('zone.js/dist/zone');


Error['stackTraceLimit'] = Infinity; // TODO: dev only

require('zone.js/dist/long-stack-trace-zone'); // TODO: dev only
